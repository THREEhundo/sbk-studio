export function generateSEOMetadata(title, description) {
	return {
		title,
		description
		//openGraph: {
		//  title,
		//  description,
		// other OpenGraph metadata
		//},
		// other SEO-related metadata
	}
}
